#!/usr/bin/env python3
"""
This file is intentionally left blank.

The HTML templates are now loaded directly from the `templates` directory
using Flask's `render_template` function. This avoids hardcoding HTML
in Python strings and ensures the GUI is always up-to-date.

See `src/web_gui/routes.py` for the implementation.
"""
